import React from "react";
import MentorCard from "../MentorCard";

const MentorCardList = () => {
  return (
    <div>
      <MentorCard data={} />
    </div>
  );
};

export default MentorCardList;

const mocCard: MentorProfileDto[] = [{}];
